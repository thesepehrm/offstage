"""offstage — background-safe autonomous QA harness for macOS apps.

Perceive through the accessibility tree, verify through ground truth, capture
real pixels with ScreenCaptureKit — all while the app under test stays in the
background and the developer keeps using the Mac. No synthetic input ever
reaches the user's foreground.
"""

__version__ = "0.1.0"
